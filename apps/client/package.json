{
  "name": "@lmntlz/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//": "The browser half. It may import @lmntlz/sim/rules and NEVER @lmntlz/sim/resolver or /ai — the resolver holds the RNG and the seed, and the AI is the ranking the engine will use against the player. The ban is in the root eslint.config.js, and packages/sim's purity.test.ts walks the whole graph behind it.",
  "//build": "`vite build` does not typecheck, so `tsc --noEmit` runs first or a type error ships as a green build.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "e2e": "playwright test",
    "//e2e": "NOT in `pnpm test` — it builds a bundle and drives a browser. Run it before a release and when the squad screen changes."
  },
  "//dependencies": "@vercel/analytics lists vue, next, nuxt, svelte and remix as peers; every one is optional and only the react entry point is imported here. Expect pnpm to say nothing about them, and do not add any of them to satisfy a warning.",
  "dependencies": {
    "@lmntlz/content": "workspace:*",
    "@lmntlz/sim": "workspace:*",
    "@vercel/analytics": "^2.0.1",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@tailwindcss/vite": "^4.1.16",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.6.1",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.1.0",
    "jsdom": "^28.0.0",
    "tailwindcss": "^4.1.16",
    "vite": "^8.1.5"
  }
}

# Keep the store strict: a package may only import what it declares.
# Phantom dependencies are how `packages/sim` would quietly acquire a server-only
# import and break the purity boundary that feature 002 exists to enforce.
shamefully-hoist=false
